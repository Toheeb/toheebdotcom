
<nav role="navigation" aria-label="Breadcrumb" class="content-block page__breadcrumb">

    <ol class="breadcrumb-list">

        {% for entry in navPages %}

            <li class="breadcrumb-list__item">
                {% if loop.last %}
                    <a href="#" aria-current="page" tabindex="-1" class="breadcrumb-list__item_last">
                        {% set pageTitle = title %}
                        
                        {% if pageTitle.length > 5 %}
                            {{ ['<span class="ellipsis">', pageTitle.slice(0, 5), '</span>', '<span class="visually-hidden">', pageTitle.slice(5), '</span>'].join('') | safe }}
                        {% else %} 
                            {{ pageTitle }}
                        {% endif %}
                    </a>
                {% else %}      
                    <a href={{entry.url | url}} class="breadcrumb-list__link">
                        {{ entry.title }}
                    </a>
                {% endif %}
            </li>

        {% endfor %}

    </ol>

</nav>